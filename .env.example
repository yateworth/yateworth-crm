# Browser-safe (VITE_ prefix = bundled into the client). Never put a secret here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_APP_BASE_URL=http://localhost:5173

# Server-only (Netlify Functions). Never reference these from src/.
SUPABASE_SERVICE_ROLE_KEY=
APOLLO_API_KEY=
EMAIL_PROVIDER=
EMAIL_PROVIDER_API_KEY=
EMAIL_WEBHOOK_SECRET=
UNSUBSCRIBE_TOKEN_SECRET=
CRON_SECRET=
SENTRY_DSN=
